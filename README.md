# COS × CI 每日观察

公开归档 [COS × CI 创意] 群里每天的 COS 侦察 / CI 侦察原文。不含密钥、账号、企微 webhook。

## 文件

`daily/YYYY-MM-DD.md`（Asia/Shanghai 日期）

每个文件两段：

- `## COS 观察`
- `## CI 观察`

当天没有合格情报也会记一句，不为凑篇幅补旧闻。

由 Grok Bot 小秘书在每天 10:00 早报流程里写入，不经过 COS Harness / COS 成本分析。
